version: "3.9"

services:
  arypodr:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - ../.env
    volumes:
      - db_data:/app/data
      - media_data:/podcasts
    restart: unless-stopped

volumes:
  db_data:
  media_data:
